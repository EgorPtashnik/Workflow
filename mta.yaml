ID: egor.ptashnik.workflow
_schema-version: 3.2.0
version: 1.0.0

modules:
  - name: workflow-project
    type: nodejs
    path: /
    parameters:
      disk-quota: 512M
      memory: 512M
      instances: 1
    build-parameters:
      ignore: ["node_modules/"]
